# Base URL for the FastAPI backend — copy to .env and adjust for your environment
VITE_API_BASE_URL=http://localhost:8000

# Supabase project — get from supabase.com → Settings → API
VITE_SUPABASE_URL=https://<project-ref>.supabase.co
VITE_SUPABASE_ANON_KEY=<anon-public-key>
